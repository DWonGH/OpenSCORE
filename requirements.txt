requests
# For eegreportparser
fuzzysearch
pandas
nltk
tqdm

# For OpenSCORE
PyQt5
psutil
pydantic>=2.6

# For eye tracker (optional, TEETACSI variant only). Not used by core OpenSCORE and has
# no wheels for Python 3.11+. Install separately on a supported Python if you need it:
#   pip install tobii_research
# tobii_research